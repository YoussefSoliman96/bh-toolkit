import { Metadata } from "next";
import ProviderForm from "../_components/ProviderForm";

const NewProviderPage = () => {
  return <ProviderForm />;
};
export const metadata: Metadata = {
  title: "Brain Health - New Provider",
};

export default NewProviderPage;
